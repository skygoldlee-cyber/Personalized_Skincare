# ref-pipeline — 참조자료 PDF → Markdown 변환 의존성
# 설치: pip install -r ref-pipeline/requirements.txt

pdfplumber>=0.11.0        # PDF 텍스트·표 추출 (필수)
PyMuPDF>=1.24.0           # 이미지 추출 (선택 — 없으면 텍스트만 변환)
# PySide6>=6.6.0          # GUI(pdf2md_gui.py) 사용 시에만 필요
