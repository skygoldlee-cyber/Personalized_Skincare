# 학습 앱 설계 가이드 (Study App Design Guide)

> **목적**: 본 문서는 "Cosmetic Pass Master" 프로젝트에서 검증된 아키텍처와 기능 패턴을 일반화하여, **다른 자격시험이나 교재를 기반으로 유사한 학습 앱을 설계할 때 참조할 수 있는 재사용 가능한 설계 가이드**로 제공합니다.
> **출처**: `ARCHITECTURE.md`, `SPEC.md`, `TEXTBOOK_AUTHORING_GUIDE.md`의 실제 구현 경험을 추출·정리
> **최종 업데이트**: 2026-09-03

---

## 목차

1. [설계 원칙](#1-설계-원칙)
2. [시스템 아키텍처](#2-시스템-아키텍처)
3. [기능 요구사항 템플릿](#3-기능-요구사항-템플릿)
4. [성능 요구사항](#4-성능-요구사항)
5. [UI/UX 요구사항](#5-uiux-요구사항)
6. [데이터 요구사항](#6-데이터-요구사항)
7. [콘텐츠 저작 규칙](#7-콘텐츠-저작-규칙)
8. [분야별 커스터마이징 포인트](#8-분야별-커스터마이징-포인트)
9. [새 교재 적용 체크리스트](#9-새-교재-적용-체크리스트)

---

## 1. 설계 원칙

| 원칙 | 내용 | 근거 |
|------|------|------|
| **Zero-Backend** | 서버, DB, 인증 없이 순수 정적 프론트엔드 | 개인 학습 도구 특성상 운영 비용·복잡성 제거, 무료 호스팅(Vercel) 활용 |
| **Vanilla First** | React/Vue 등 프레임워크 미사용, 순수 HTML/CSS/JS (ESM) | 빌드 스텝 제거, 장기 유지보수 시 프레임워크 종속성 리스크 제거, 번들 크기 최소화 |
| **Offline-Capable PWA** | Service Worker로 App Shell + 학습 데이터 캐시 | 이동 중/무인터넷 환경에서도 학습 가능, 설치형 PWA로 홈 화면 추가 |
| **Mobile-First Responsive** | 모바일 하단 탭 바 ↔ 데스크톱 사이드바 적응형 | 실제 사용자 대부분이 스마트폰 접속 |
| **Content-Agnostic** | `manifest.json`을 SSOT로 사용, 교재 전체 교체 시 소스 수정 최소화 | 콘텐츠와 코드의 분리, 재사용성 극대화 |

---

## 2. 시스템 아키텍처

### 2.1 계층 구조

```
Browser (Client)
├── Presentation Layer    — index.html (App Shell) + CSS (디자인 시스템)
├── Application Layer     — app.js (초기화·라우팅·이벤트 위임)
│   ├── State Management  — state.js (전역 상태, localStorage 영속성)
│   ├── Data Loader       — data-loader.js (온디맨드 과목 로딩)
│   ├── Parsers           — textbook-parser.js, markdown-parser.js
│   ├── View Controllers  — views/*.js (각 기능별 뷰 모듈)
│   └── Security          — sanitize.js (XSS 방어)
├── Data Layer            — registry.js (빌드 생성), ingredients_data.js
├── Persistence Layer     — localStorage (학습 진행) + Cache Storage (SW)
└── Service Worker        — sw.js (오프라인 캐싱, 7단계 분기 전략)
```

### 2.2 호스팅

| 항목 | 방안 |
|------|------|
| 정적 호스팅 | Vercel 무료 티어 (CDN, 커스텀 도메인, 보안 헤더) |
| 대용량 미디어 | 외부 CDN (오디오북 등, SW 캐시 제외) |
| 파일 크기 제약 | 100MB/파일 — 대용량 콘텐츠는 런타임 fetch + MD 변환으로 회피 |

### 2.3 의존성 최소화

| 항목 | 방안 |
|------|------|
| 런타임 라이브러리 | 없음 (차트, 다이어그램 모두 자체 구현 또는 온디맨드) |
| 온디맨드 라이브러리 | Mermaid.js (다이어그램 블록이 있을 때만 동적 로드) |
| 폰트/아이콘 | 자체 호스팅 (오프라인 지원) |
| 개발 의존성 | Vitest + jsdom (테스트), Node.js (빌드 도구) |

---

## 3. 기능 요구사항 템플릿

다음은 자격시험 대비 학습 앱의 표준 기능 세트입니다. 교재 성격에 따라 선택적 적용 가능합니다.

### 3.1 대시보드 (Dashboard)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| D-01 | 전체 학습 통계 (외운 카드, 오답, 퀴즈 성적, 스트릭) | 필수 |
| D-02 | 과목별 학습 진행률 카드 (registry 기반 동적 생성) | 필수 |
| D-03 | 과목 바로가기 버튼 | 필수 |
| D-04 | 추천 링크/유튜브 채널 카드 (manifest.json `resources` 기반) | 선택 |
| D-05 | 일일 챌린지 모달 | 선택 |

### 3.2 플래시카드 (Flashcard)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| F-01 | 3D 플립 카드 애니메이션 (perspective, preserve-3d) | 필수 |
| F-02 | GPU 가속 (`will-change: transform`) | 필수 |
| F-03 | 키보드 접근성 (`role="button"`, `tabindex="0"`, Enter/Space) | 필수 |
| F-04 | "외웠어요" / "헷갈려요" 분류 (Set 기반 O(1) 조회) | 필수 |
| F-05 | 카드 셔플 (Fisher-Yates) | 선택 |
| F-06 | 과목별 필터링 | 필수 |

### 3.3 퀴즈 (Quiz)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| Q-01 | 빈칸 채우기 퀴즈 (교재 본문에서 자동 추출) | 필수 |
| Q-02 | 객관식 확인문제 (단원 말미 4지선다) | 선택 |
| Q-03 | 즉각 피드백 (정답/오답, `aria-live`) | 필수 |
| Q-04 | 오답 노트 관리 (틀린 문제 자동 수집) | 필수 |
| Q-05 | 문제 셔플 | 선택 |
| Q-06 | 일일 챌린지 | 선택 |

### 3.4 모의고사 (Exam Simulator)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| E-01 | 실전 모의고사 (타이머, OMR 카드, 채점) | 선택 |
| E-02 | 과목별 모의고사 (registry 기반 동적 생성) | 선택 |
| E-03 | 중단된 모의고사 이어하기 | 선택 |
| E-04 | 오답 복습 모의고사 | 선택 |
| E-05 | 성적 기록 및 추세 분석 (꺾은선 그래프) | 선택 |
| E-06 | 합격/과락 판정 | 선택 |

### 3.5 교재 리더 (Textbook Reader)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| TR-01 | 마크다운 교재 본문 HTML 렌더링 (런타임 MD 파싱) | 필수 |
| TR-02 | 과목별 단원 목차 (사이드바/드롭다운) | 필수 |
| TR-03 | 단원 간 이동 (이전/다음) | 필수 |
| TR-04 | 스크롤 위치 기억/복원 | 필수 |
| TR-05 | 기출/중요 마커 하이라이트 | 선택 |
| TR-06 | Mermaid 다이어그램 렌더링 (온디맨드 로드) | 선택 |
| TR-07 | 마크다운 링크 파싱 (`[text](url)` → `<a>`) | 필수 |

### 3.6 학습 보조 도구

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| SA-01 | 기출 필터 & 요약 (기출 섹션 하이라이트 + 비기출 디밍) | 선택 |
| SA-02 | 숫자·기한 빈칸 카드 (정규식 자동 추출) | 선택 |
| SA-03 | 용어집 자동 생성 및 본문 키워드 자동 링크 | 선택 |
| SA-04 | 용어집 점프 후 "원래 위치로" 복귀 버튼 | 선택 |

### 3.7 교재 검색 (Textbook Search)

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| TS-01 | 교재 본문 전문 검색 (모든 과목/단원) | 필수 |
| TS-02 | 검색 인덱스 사전 구축 (재계산 방지) | 필수 |
| TS-03 | 디바운스 (250ms, 모바일 타이핑 랙 감소) | 필수 |
| TS-04 | 검색 결과 마크다운 렌더링 (코드블록/테이블/머메이드) | 필수 |
| TS-05 | 검색 결과 카드 토글 (더 보기/접기) | 선택 |

### 3.8 참조자료 연결

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| RR-01 | 참조자료 HTML/MD 뷰어 오버레이 (fetch + 파싱, iframe 없음) | 선택 |
| RR-02 | 키워드 기반 스크롤 (법령 조문 등) | 선택 |
| RR-03 | 본문 키워드 자동 하이퍼링크 | 선택 |
| RR-04 | 인라인 프리뷰 툴팁 (hover/long-press) | 선택 |
| RR-05 | sessionStorage 캐싱 (24h TTL) | 선택 |
| RR-06 | 인쇄 기능 | 선택 |
| RR-07 | 중앙 설정 모듈 (과목 변경 시 1개 파일만 수정) | 필수 |

### 3.9 데이터 백업/복원

| ID | 요구사항 | 필수/선택 |
|----|---------|-----------|
| B-01 | 학습 진행 상황 JSON 내보내기 | 필수 |
| B-02 | JSON 가져오기 (화이트리스트 검증) | 필수 |
| B-03 | 악성 키 주입 방지 | 필수 |

### 3.10 분야 특화 기능 (선택)

| ID | 요구사항 | 적용 조건 |
|----|---------|-----------|
| DI-01 | 성분/용어 사전 검색 (분류별, 초성 지원) | 화학/식약/의료 분야 |
| T-01 | 계산 훈련 문제 생성기 (배합량, 농도 등) | 계산 문제가 있는 시험 |
| T-02 | 뽀모도로 타이머 | 범용 |
| T-03 | 손글씨 연습장 (Canvas) | 범용 |
| A-01 | 오디오북 (단원별 MP3, Media Session API) | 오디오 콘텐츠가 있는 경우 |

---

## 4. 성능 요구사항

| ID | 요구사항 | 목표 |
|----|---------|------|
| PF-01 | 런타임 MD 파싱 (재빌드 불필요) | 교재 수정 시 빌드만으로 반영 |
| PF-02 | 온디맨드 과목 로딩 | 필요한 과목만 로드, 초기 로드 최소화 |
| PF-03 | `file://` 폴백 번들 과목별 분할 | 로컬 파일 열기 시 fetch 차단 우회 |
| PF-04 | 참조자료 sessionStorage 캐싱 (24h TTL) | 재방문 시 fetch 0회 |
| PF-05 | 검색 인덱스 사전 구축 | 검색마다 재계산 방지 |
| PF-06 | 디바운스 (250ms) | 모바일 타이핑 랙 감소 |
| PF-07 | 대시보드 통계 O(1) 조회 | 과목별 카운트 맵 캐싱 |
| PF-08 | Mermaid.js 온디맨드 로드 | 다이어그램 블록이 있을 때만 3.3MB 로드 |
| PF-09 | 전역 테이블 가로 스크롤 | CSS 전역 규칙으로 중복 스타일 제거, 렌더링 일관성 |
| PF-10 | `console.log` → `console.debug` | 프로덕션 콘솔 노이즈 제거 |

---

## 5. UI/UX 요구사항

### 5.1 반응형 & 모바일

| ID | 요구사항 | 목표 |
|----|---------|------|
| R-01 | 적응형 네비게이션 (데스크톱 사이드바 ↔ 모바일 하단 탭 바) | 화면 크기에 맞는 최적의 내비게이션 |
| R-02 | 동적 뷰포트 (`100dvh`) | 주소창 표시/숨김 레이아웃 점프 방지 |
| R-03 | Safe Area 대응 (`env(safe-area-inset-bottom)`) | iOS 노치/홈 인디케이터 회피 |
| R-04 | 스크롤 위치 복원 | 뷰 전환 시 사용자 위치 보존 |
| R-05 | 그리드 종열 전환 | 데스크톱 다열 → 모바일 단일 열 |
| R-06 | 전역 테이블 가로 스크롤 | 넓은 표의 셀 찌그러짐 방지, wrapper 기반 스크롤 |

### 5.2 접근성

| ID | 요구사항 | 기준 |
|----|---------|------|
| A-01 | ARIA 속성 (`role`, `aria-modal`, `aria-label`, `aria-live`) | WCAG 2.1 AA |
| A-02 | 플래시카드 키보드 지원 (`role="button"`, `tabindex="0"`, Enter/Space) | 키보드만으로 모든 기능 사용 가능 |
| A-03 | 라이트 모드 WCAG AA 대비 (배지 색상 4.5:1 이상) | 텍스트 가독성 |
| A-04 | 터치 타겟 최소 44×44px | 모바일 터치 정확도 |

### 5.3 테마 시스템

| ID | 요구사항 | 목표 |
|----|---------|------|
| TH-01 | CSS 변수 기반 듀얼 테마 (다크 기본, 라이트 토글) | 사용자 선호 존중 |
| TH-02 | FOUC 방지 (`<head>` 인라인 테마 적용, 페인트 전) | 깜빡임 없는 테마 적용 |
| TH-03 | 테마 결정 우선순위: `localStorage` > `prefers-color-scheme` > 다크 | 사용자 설정 > 시스템 설정 > 기본값 |
| TH-04 | `themechange` 커스텀 이벤트 브로드캐스트 | 모듈 간 테마 동기화 |
| TH-05 | Mermaid 다이어그램 테마별 렌더링 (dark/default) | 테마 일관성 |

### 5.4 보안

| ID | 요구사항 | 기준 |
|----|---------|------|
| S-01 | CSP `script-src 'self'` (인라인 스크립트 차단) | XSS 방어 |
| S-02 | 이벤트 위임 패턴 (`data-click`/`data-arg`/`data-input`) | 인라인 핸들러 없이 이벤트 처리 |
| S-03 | XSS 방어 (`sanitize.js`, 텍스트 정제 + 이스케이프) | 사용자 입력/외부 콘텐츠 안전 처리 |
| S-04 | 백업 복원 화이트리스트 (`ALLOWED_KEYS`) | 악성 데이터 주입 방지 |
| S-05 | 보안 헤더 (`X-Content-Type-Options`, `Referrer-Policy`, `X-Frame-Options: DENY`) | Vercel `vercel.json` 설정 |

### 5.5 PWA & 오프라인

| ID | 요구사항 | 목표 |
|----|---------|------|
| P-01 | Service Worker 기반 오프라인 캐싱 (7단계 분기 전략) | 무인터넷 환경에서 완전 학습 |
| P-02 | App Shell + 데이터 번들 프리캐시 | 첫 로드 후 오프라인 가능 |
| P-03 | `skipWaiting()` + `controllerchange` 자동 리로드 | 업데이트 즉시 적용 |
| P-04 | `CACHE_VERSION` 관리 (배포마다 버전업, 구 캐시 자동 정리) | 캐시 스큐 방지 |
| P-05 | PWA 설치 프롬프트 (`beforeinstallprompt` 조기 캡처) | 설치 유도 |
| P-06 | 인앱 브라우저(WebView) 감지 + "Chrome으로 열기" 안내 | 설치 불가 환경 안내 |
| P-07 | 자가 복구 메커니즘 (SW update → 하드 리셋 → 수동 복구) | 장애 자동 복구 |

---

## 6. 데이터 요구사항

### 6.1 데이터 아키텍처

| ID | 요구사항 | 방안 |
|----|---------|------|
| DA-01 | `manifest.json`을 SSOT로 사용 | 과목, 단원, 시험, 추천 링크, UI 텍스트 |
| DA-02 | 시험/사전 데이터: 빌드 타임 해시드 JS 번들 | `<script>` 로드, 빌드 시 생성 |
| DA-03 | 교재/카드/퀴즈: 런타임 MD fetch + 파싱 | 재빌드 불필요, 콘텐츠만 교체 가능 |
| DA-04 | `file://` 폴백: 과목별 분할 JS 번들 | 로컬 파일 열기 시 fetch 차단 우회 |
| DA-05 | 사용자 진행 상황: `localStorage` 영속화 | 계정/로그인 불필요 |

### 6.2 안정적 ID 체계

| ID | 요구사항 | 방안 |
|----|---------|------|
| ID-01 | 카드 ID: `sha256(subjectKey\|chapterKey\|term)` 앞 6자리 | 오탈자 수정 시 ID 불변 (정의 본문 제외) |
| ID-02 | 퀴즈 ID: `sha256(term\|answer)`, 중복 시 순번 결합 | 동일 정답 충돌 회피 |
| ID-03 | 고아 진행상황 정리 | 삭제된 카드 ID 자동 감지/제거 |

### 6.3 빌드 파이프라인

| ID | 요구사항 | 방안 |
|----|---------|------|
| BP-01 | 빌드 스크립트: registry, exam bundles, ingredients 생성 | `npm run build:data` |
| BP-02 | 스키마 검증 (카드/단원 수, 고유 ID 중복, 급감 경고) | 데이터 품질 보증 |
| BP-03 | 매니페스트 자체 검증 (`chapters[].file` 존재, 정합성) | 설정 오류 사전 감지 |
| BP-04 | 빌드 파서 ↔ 런타임 파서 등가성 검증 | 파싱 일관성 보증 |
| BP-05 | SW 캐시 버전 자동 스탬프 | 배포마다 자동 버전업 |

### 6.4 manifest.json 구조 (템플릿)

```json
{
  "schemaVersion": 1,
  "contentYear": "2026",
  "subjects": [
    {
      "key": "subjectKey",        // 고유 식별자 (영문)
      "order": 1,                  // 과목 순서
      "name": "과목 전체 이름",
      "shortName": "약칭",
      "dir": "교재/subjectKey",    // content/ 하위 디렉토리
      "chapters": [
        {
          "key": "full",
          "title": "챕터 제목",
          "file": "1과목_파일명_표준형.md",
          "storyFile": "1과목_파일명_이야기형.md"  // 선택
        }
      ]
    }
  ],
  "exams": [
    {
      "key": "subject1",
      "subject": "subjectKey",     // 위 subjects[].key와 매칭
      "part": 1,
      "title": "문제은행 제목",
      "file": "과목1_문제은행_교재인용.md"
    }
  ],
  "uiText": {
    "dashboard": { "title": "학습 대시보드", "subtitle": "{year} 시험 합격을 위한 분석 및 스마트 툴" },
    "exam": { "title": "실전 모의고사", "subtitle": "교재 인용 {totalQuestions}제 문제은행" }
    // ... 각 뷰별 UI 텍스트
  },
  "resources": {
    "sectionTitle": "추천 학습 자료",
    "links": [
      { "title": "추천 채널", "url": "https://...", "linkText": "바로가기" }
    ]
  }
}
```

---

## 7. 콘텐츠 저작 규칙

### 7.1 디렉토리 구조 (템플릿)

```
content/
├── manifest.json              ← SSOT: 과목/단원/파일 정의
├── 교재/
│   ├── subject1/              ← 과목 1 폴더 (key와 매칭)
│   │   ├── 1과목_제목_표준형.md
│   │   └── 1과목_제목_이야기형.md   ← 선택
│   ├── subject2/
│   └── ...
├── 문제은행/
│   ├── 과목1_문제은행_교재인용.md
│   └── ...
├── 참조자료/                   ← 학습 보조 자료
│   ├── 공통/                   ← 공통 참조자료
│   ├── 법령원문/               ← 법령 원문 (해당 시험에 법령이 있는 경우)
│   ├── 과목1/                  ← 과목별 참조자료
│   └── 원료/                   ← 분야 특화 자료 (성분 등)
└── 학습안내서.md
```

### 7.2 교재 본문 Markdown 규칙

| 헤더 | 용도 | 파서 동작 |
|------|------|-----------|
| `#` | 교재 제목 (파일당 1개) | `chapterTitle` 추출 |
| `##` | 섹션 (단원/주제) | 카드 `category`, 교재 리더 섹션 분할 |
| `###` | 서브 섹션 | 참조용, 카드 추출에 영향 없음 |

### 7.3 카드 추출 규칙

- **표**: 첫 번째 열 = 용어(term), 두 번째 열 = 정의(definition)
- **리스트**: `🔖기출` 또는 `📌중요` 마커가 있는 항목만 카드로 추출
- **자동 제외**: 용어 3자 이하/50자 초과, 정의 10자 이하, 범용 표현, 숫자만 있는 용어

### 7.4 퀴즈 자동 생성 규칙

1. `**볼드**` 텍스트 → 빈칸 채우기 퀴즈
2. 숫자+단위 패턴 → 빈칸 채우기 퀴즈
3. 둘 다 없음 → 용어 맞추기 퀴즈 (정의 → 용어)

### 7.5 학습 보조 요소 (권장)

각 `##` 섹션에 배치 권장:

| 요소 | 형식 | 효과 |
|------|------|------|
| 학습 가이드 | `> 📋 출제 빔도: ★★★ 예상 소요: 60분 핵심 키워드: ...` | 학습 방향 설정 |
| 한 줄 핵심 | `> ### 🎯 한 줄 핵심: ...` | 섹션 요약 |
| 출처 표기 | `> 📌 출처: 법령 제N조 | 참조 PDF: ...` | 근거 명시 |
| 비교표 | `| 구분 | A | B | C |` | 개념 대조 |
| 확인문제 | `> 📝 Q. ... (정답: ...)` | 즉시 확인 |

### 7.6 Mermaid 다이어그램 (선택)

교재 MD에 ```` ```mermaid ```` 코드블록으로 작성하면 온디맨드 로드 + 자동 렌더링됩니다. 타입은 `src/mermaid-utils.js`가 자동 감지합니다.

| 타입 | 용도 | 문법 | CSS 클래스 |
|------|------|------|------------|
| mindmap | 계층 구조 시각화 | `mindmap` + 들여쓰기 (root 1 space, 각 레벨 +1) | `mermaid-mindmap` |
| flowchart | 절차/흐름 시각화 | `flowchart TD` + 노드/화살표 | `mermaid-flowchart` |
| sequenceDiagram | 상호작용 순서 (신고→심사→승인) | `sequenceDiagram` + participant/메시지 | `mermaid-other` |
| gantt | 일정/타임라인 (시험 준비, 법령 시행) | `gantt` + dateFormat/section | `mermaid-other` |
| pie | 비율 시각화 (출제 비율, 배점) | `pie title ...` + 데이터 | `mermaid-other` |
| timeline | 연혁/제도 변천 | `timeline` + 연도/이벤트 | `mermaid-other` |
| stateDiagram-v2 | 상태 변화 (허가 절차, 제품 수명) | `stateDiagram-v2` + 상태/전이 | `mermaid-other` |
| gitGraph | 개정 이력 (법령 변천) | `gitGraph` + commit/branch | `mermaid-other` |
| quadrantChart | 2축 매트릭스 (효능 vs 위험도) | `quadrantChart` + 축/데이터 | `mermaid-other` |
| classDiagram | 클래스/구조 관계 | `classDiagram` + 클래스 정의 | `mermaid-other` |
| erDiagram | 엔티티 관계 (법령 간 참조) | `erDiagram` + 엔티티/관계 | `mermaid-other` |
| sankey | 흐름/분배 (원료→배합→제품) | `sankey` + 소스/타겟/값 | `mermaid-other` |

> **스타일 분리**: flowchart만 `lineWidth: 1` 등 세부 themeVariables 적용, mindmap은 기본 스타일 유지, 기타 타입은 Mermaid 기본 테마 + 텍스트 대비 보정만 적용

### 7.7 이야기형 교재 (선택)

- 파일명 규칙: `{기본파일명}_이야기형.md`
- 동일한 `##` 섹션 구조 유지
- 서사는 `### 📖` 서브헤더 아래에 배치 (카드/퀴즈 추출에 영향 없음)
- 파일이 없으면 자동으로 기본 교재 파일 표시

### 7.8 문제은행 Markdown 규칙

```
# 제N과목: 과목명 실전 예상 문제

## 📝 [객관식 섹션]

### Q1. 문제 내용...
① 보기1
② 보기2
③ 보기3
④ 보기4
⑤ 보기5

---

## 🔑 정답 및 교재 근거

**Q1.**
> **정답: ③** (교재: L250)
> **📖 교재 근거:** ...
```

---

## 8. 분야별 커스터마이징 포인트

새로운 자격시험/교재에 적용할 때 수정이 필요한 파일을 등급별로 정리합니다.

### 8.1 필수 수정 (콘텐츠 교체)

| 파일 | 수정 내용 | 시점 |
|------|-----------|------|
| `content/manifest.json` | 과목 key, 이름, 파일 경로, 시험 정보, UI 텍스트 | 새 교재 적용 시 |
| `content/교재/*.md` | 교재 본문 | 새 교재 작성 |
| `content/문제은행/*.md` | 문제은행 | 새 문제 작성 |
| `content/참조자료/` | 참조자료 PDF/MD | 해당 시험의 법령/자료 |
| `src/pdf-registry.js` | 참조자료 매핑, 키워드 링크 | 참조자료 변경 시 |
| `index.html` | `<title>`, 자격증명, 과목명 | 자격증 변경 시 |

### 8.2 조건부 수정 (분야 변경 시에만)

| 파일 | 수정 내용 | 조건 |
|------|-----------|------|
| `src/study-aids.js` | 단위 목록(`%`, `g`, `ml` 등), 키워드 리스트 | 완전히 다른 분야 교재로 변경 시 |
| `src/trainer-calc.js` | 계산 문제 유형 | 계산 문제가 있는 시험인 경우 |
| `src/reader-format.js` | 참조자료 폴더명 패턴 | 폴더 구조 변경 시 |
| `content/참조자료/원료/` | 성분 데이터 | 화학/식약 분야인 경우 |

### 8.3 수정 불필요 (재빌드만)

| 파일 | 이유 |
|------|------|
| `src/keyword-index.js` | 빌드 도구가 자동 생성 |
| `src/app.js` | `manifest.json` `uiText`에서 동적 조회 |
| `data/registry.js` | 빌드 시 자동 생성 |
| `sw.js` | 빌드 시 `CACHE_VERSION` 자동 스탬프 |

### 8.4 요약 매트릭스

| 파일 | 같은 분야 교재 변경 | 다른 분야 교재 변경 |
|------|-------------------|-------------------|
| `manifest.json` | ⚠️ 수정 (파일 경로) | ⚠️ 수정 (전체 재작성) |
| `pdf-registry.js` | ⚠️ 수정 (참조자료 매핑) | ⚠️ 수정 |
| `index.html` | ✅ 수정 불필요 (연도 제외) | ⚠️ 자격증명 수정 |
| `study-aids.js` | ✅ 수정 불필요 | ⚠️ 단위/키워드 수정 |
| `trainer-calc.js` | ✅ 수정 불필요 | ⚠️ 계산 유형 수정 |
| `keyword-index.js` | ✅ 재빌드만 | ✅ 재빌드만 |

---

## 9. 새 교재 적용 체크리스트

### 9.1 콘텐츠 준비

- [ ] `manifest.json`의 `subjects[].key`/`dir`/`file` 경로가 실제 파일 위치와 일치하는가?
- [ ] `manifest.json`의 `exams[].subject`가 `subjects[].key`와 매칭되는가?
- [ ] `manifest.json`의 `uiText`가 새 자격증명/과목명에 맞게 수정되었는가?
- [ ] 교재 MD 파일의 `#` 헤더가 파일당 1개인가?
- [ ] 카드 추출을 원하지 않는 `##` 섹션에 제외 마커(`🧭`, `📊` 등)가 있는가?
- [ ] 표의 첫 번째 열이 용어(term), 두 번째 열이 정의(definition)인가?
- [ ] 중요 카드에 `🎯 기출` 또는 `📌 중요` 마커가 있는가?
- [ ] 문제은행 정답 섹션이 `## 🔑 정답 및 교재 근거` 아래에 있는가?

### 9.2 분야 특화

- [ ] `pdf-registry.js`의 참조자료 매핑이 새 교재의 법령/자료에 맞는가?
- [ ] `study-aids.js`의 단위 목록(`%`, `g`, `ml` 등)이 새 분야에 적합한가?
- [ ] `trainer-calc.js`의 계산 유형이 새 시험의 계산 문제에 맞는가? (해당 시험에 계산 문제가 있는 경우)
- [ ] `index.html`의 `<title>`과 자격증명이 수정되었는가?

### 9.3 빌드 및 검증

- [ ] `npm run build:data`가 오류 없이 완료되는가?
- [ ] 빌드 로그에서 카드/퀴즈 수가 예상 범위 내인가?
- [ ] 파서 등가성 검사가 통과하는가?
- [ ] `sw.js`의 `CACHE_VERSION`이 자동 bump되었는가?
- [ ] `npm test`가 모두 통과하는가?

### 9.4 배포 전 확인

- [ ] 오프라인 모드에서 모든 기능이 동작하는가?
- [ ] 모바일/데스크톱 레이아웃이 정상인가?
- [ ] PWA 설치가 정상적으로 동작하는가?
- [ ] CSP 위반 에러가 콘솔에 없는가?

---

## 관련 문서

- [`ARCHITECTURE.md`](ARCHITECTURE.md) — 본 프로젝트의 상세 아키텍처 및 설계 철학
- [`SPEC.md`](SPEC.md) — 본 프로젝트의 요구사양 명세서 (구현 완료 기준)
- [`TEXTBOOK_AUTHORING_GUIDE.md`](TEXTBOOK_AUTHORING_GUIDE.md) — 교재 Markdown 작성 지침
- [`DEPLOYMENT_GUIDE.md`](DEPLOYMENT_GUIDE.md) — Vercel 배포 가이드
- [`TESTING.md`](TESTING.md) — 테스트 가이드
