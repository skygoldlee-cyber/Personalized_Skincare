# 오디오북 파이프라인 필수 패키지
# 설치: pip install -r requirements.txt

# ElevenLabs TTS API 클라이언트 (--polish-only 모드에서는 불필요)
elevenlabs>=1.0.0

# 참고: MP3 병합은 시스템에 설치된 ffmpeg를 우선 사용합니다.
# ffmpeg가 없으면 바이너리 연결로 폴트백되므로 pydub 등은 필수 아님.
# ffmpeg 설치 (Windows): winget install ffmpeg  또는  https://ffmpeg.org/download.html
